import { Request, Response } from "express";

export default {
  async createMarker(req: Request, res: Response) {
    const { zip, number, complement, latitude, longitude } = req.body;

    
      

  },
};
